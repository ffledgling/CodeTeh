#!/usr/bin/python

import random


class Numbers:

    def __init__(self)
        x = range(0,10)
        random.shuffle(x)
        self.numbers = x

    def sortNumbers(self):
        self.numbers.sort()
